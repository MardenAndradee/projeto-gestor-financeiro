# Copie para .env (nunca commitado) e exporte antes de rodar a API,
# ou configure como env vars na sua IDE (IntelliJ Run Configuration).
DB_URL=jdbc:postgresql://localhost:5432/projeto
DB_USERNAME=postgres
DB_PASSWORD=
JWT_SECRET=
MAIL_USERNAME=
MAIL_PASSWORD=
